[project]
name = "florascan"
version = "0.1.0"
description = "FloraScan - AI plant identification"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
]

[project.scripts]
app = "app:app"
